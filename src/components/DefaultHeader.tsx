import {Text, View} from 'react-native';

export default function DefaultHeader({}): React.JSX.Element {
  return (
    <View
      style={{
        height: 60,
      }}>
      <Text>Hello</Text>
    </View>
  );
}
